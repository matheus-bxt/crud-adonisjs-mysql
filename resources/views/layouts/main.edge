<!DOCTYPE html>
<html lang="pt-br">
    <head>
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
        <link rel="icon" href="/img/title-icon.png">
        <title>CRUD</title>

        <!-- Axios -->
        {{ script('/js/axios.min.js')}}

        <!-- jQuery -->
        {{ script('/js/jquery.js') }}

        <!-- Principal JS do Bootstrap -->
        {{ script('/js/bootstrap.min.js') }}

        <!-- Principal CSS do Bootstrap -->
        {{ style('/bootstrap/bootstrap.min.css') }}

        <!-- Font-Awesome -->
        {{ style('/font-awesome/css/all.css') }}

        <!-- Estilos customizados para esse template -->
        {{ style('/css/main.css') }}
        
    </head>
    <body>
        <nav class="navbar navbar-expand-md navbar-dark fixed-top bg-dark navbar-color">
            <div class="collapse navbar-collapse" id="navbarCollapse">
                <ul class="navbar-nav mr-auto">
                    <li class="nav-item" id="itemIndice">
                        <a class="nav-link" href="/">Índice</a>
                    </li>
                    <li class="nav-item" id="itemHospitais">
                        <a class="nav-link" href="hospitais">Hospitais</span></a>
                    </li>
                    <li class="nav-item" id="itemEnfermeiras">
                        <a class="nav-link" href="enfermeiras">Enfermeiras</a>
                    </li>
                </ul>
                <ul class="navbar-nav">
                    <li class="nav-item" id="itemSair">
                        <a class="nav-link" href="login">Sair</a>
                    </li>
                </ul>                
            </div>
        </nav>
        
        <section class="section">
            <div class="container">
                @!section('content')
            </div>
        </section>

        <script>

            // Ativa nav-item corrente no load do documento
            window.addEventListener('load', function(event) {
                if(document.location.pathname == '/') {
                    var navItem = document.getElementById('itemIndice');
                    navItem.classList.add('active');
                } else if(document.location.pathname == '/hospitais') {
                    var navItem = document.getElementById('itemHospitais');
                    navItem.classList.add('active');
                } else if(document.location.pathname == '/enfermeiras') {
                    var navItem = document.getElementById('itemEnfermeiras');
                    navItem.classList.add('active');
                }
            });

        </script>

    </body>
</html>